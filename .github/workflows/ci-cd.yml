name: CI - Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.kritika2227 }}
          password: ${{ secrets.Kritika!@#$1234 }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/ecommerce-app:latest

  deployments-tests:
    runs-on: ubuntu-latest
    needs: build-and-push   # ensures this job runs only after Docker build/push completes
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Deployment Tests
        run: |
          echo "Add your deployment test commands here"
          # For example: curl endpoints, smoke tests, etc.

  complete-demo-sync-check:
    runs-on: ubuntu-latest
    needs: build-and-push   # also runs after Docker build/push
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Complete Demo Sync Check
        run: echo "Demo sync check completed!"
